{
  "buildCommand": "cd api && npm run vercel-build && cd ../frontend && npm run build",
  "outputDirectory": "frontend/dist",
  "installCommand": "cd api && npm install && cd ../frontend && npm install",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/serverless/[...path]"
    }
  ]
}
